# 经典案例

## 同一中文字段在不同查询链路中表现不同

**症状**：用户提供的 MySQL 命令行结果可见内置定时任务的中文 `task_name`，而其他客户端或接口返回空，非中文字段正常。

**调查教训**：首轮应纳入数据流与查询编码/解码假设。不能仅因接口返回空就断言数据库存储为空，也不能仅凭不同时刻的查询结果断言连接了不同数据库。

**判别观察**：核对同一实例、同一主键、相近时刻的 `HEX`、`LENGTH`、`CHAR_LENGTH`，对比查询会话字符集及各层读到的字段值，定位中文首次变空的边界。命令行显示中文是线索；具体异常环节仍须由证据确认。
