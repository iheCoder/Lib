你是首次接触这些需求文档的开发者。只依据每份文档回答下列业务问题，不读取任何文件，不使用工具，不推测未说明的规则。无法从文档确定时回答 UNKNOWN 并说明歧义。每个答案引用文档中的短证据。不同文档独立回答，不把另一文档信息补到本份里。
只输出 JSON：{"documents":[{"run":"...","answers":[{"question":"...","answer":"...","quote":"..."}]}]}。
问题：
["普通成员能导出吗？", "可以跨团队导出吗？", "空列表和导出失败分别怎样？"]
文档：
[{"run": "c05-r1", "document": "**成员列表导出需求**\n\n为方便团队线下核对成员信息，支持团队管理员将当前团队的成员列表导出为 CSV 文件。\n\n**核心规则**\n\n- 只有当前团队的管理员可以导出，普通成员不能导出。\n- 导出范围以当前可见成员列表为准，仅包含当前团队成员。\n- 文件仅包含“姓名”和“成员身份”两列，不包含手机号、邮箱等其他字段。\n- 当前可见成员列表为空时，仍可导出 CSV 文件，文件只包含表头。\n- 导出失败时，向用户提示失败，不提供不完整文件。\n\n**验收要点**\n\n- 团队管理员可以成功导出符合上述范围和字段要求的 CSV 文件。\n- 普通成员无法成功导出成员列表。\n- 空列表导出结果仅有“姓名”和“成员身份”表头。\n- 导出失败时有失败提示，且不会向用户提供不完整文件。\n\n**本次范围外**\n\n跨团队导出、定时导出、邮件发送。"}, {"run": "c05-r2", "document": "团队需要导出成员列表，用于线下核对。本次需求如下：\n\n- **使用权限**：只有团队管理员可以导出当前团队的成员列表，普通成员不能导出。原型中“所有当前团队成员均可导出”的规则已失效。\n- **导出范围**：以导出时当前可见成员列表为范围，仅包含当前团队成员。\n- **文件内容**：格式为 CSV，仅包含“姓名”和“成员身份”两列，不含手机号、邮箱等其他字段。\n- **正常流程**：团队管理员在成员列表使用“导出成员”功能，成功后获得符合上述范围和字段要求的完整 CSV 文件。\n- **空列表**：仍可导出，文件仅包含表头。\n- **失败处理**：提示导出失败，不提供不完整文件。\n- **本次不做**：跨团队导出、定时导出、邮件发送。\n\n验收时应确认：管理员可成功导出，普通成员不能导出；导出范围与当前可见列表一致；文件仅含指定两列；空列表只生成表头；失败时有提示且不会提供不完整文件。"}, {"run": "c05-r3", "document": "成员列表导出用于团队线下核对，仅团队管理员可以导出。\n\n**核心需求**\n\n- 团队管理员可在成员列表中执行“导出成员”，获得 CSV 文件。\n- 导出以操作时当前可见的成员列表为范围，仅包含当前团队成员。\n- 文件仅包含“姓名”和“成员身份”两列，不包含手机号、邮箱等其他字段。\n\n**权限与特殊情况**\n\n- 普通成员不能导出成员列表；导出权限以业务说明为准，过时原型中的全员可导出规则不再适用。\n- 当前可见成员列表为空时，仍导出 CSV 文件，仅包含表头。\n- 导出失败时提示失败，不提供不完整文件。\n\n**本次范围外**\n\n跨团队导出、定时导出、邮件发送。"}]