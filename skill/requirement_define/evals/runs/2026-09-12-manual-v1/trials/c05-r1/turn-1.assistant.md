目前已明确：为线下核对提供当前团队成员列表的 CSV 导出，仅包含姓名和成员身份；范围以当前可见成员列表为准，空列表只导出表头；失败时提示失败，不提供不完整文件。本次不做跨团队导出、定时导出或邮件发送。

两份材料在**导出权限**上存在直接冲突：

| 材料 | 权限规则 | 对普通成员的影响 |
|---|---|---|
| materials/business.md | 只有团队管理员能导出 | 不能导出 |
| materials/prototype.md | 所有当前团队成员都能使用按钮并成功导出 | 可以导出 |

两份材料没有指定优先级，也无法确认版本先后，因此不能自行采用其中一份。这个冲突影响实际导出权限，不能仅通过调整按钮显示来消除。

请确认：**当前团队中，哪些成员应有权导出成员列表？**