# 运行环境记录

- 正式被测运行：桌面应用随附 Codex CLI 0.153.4，模型 gpt-6-astra，reasoning=medium；每案例新会话，后续轮次 resume 同一会话。
- 未更改用户全局配置；调用时忽略用户 config，显式保留本机已有 respect_system_proxy 网络选项，并禁用 memories、其他 skills、plugins、apps、hooks、multi_agent。技能通过工作目录中的 SKILL.md 显式加载，不验证自动发现/触发能力。
- 正式试验只测当前 skill，12 案例各 3 次。每轮最多 240 秒；未使用固定提问数作为通过标准。正式结果以实际 turn.completed 为准。
- 参与者工作区为 /private/tmp/requirement-define-eval-20260912/cNN-rN，只含当前案例初始输入、公开材料和 skill。后续答案只由主持人按时发入消息，不复制到参与者目录。
- CLI 子进程使用 read-only 沙箱，额外开发指令限定只读当前工作区。宿主仍具备广泛读取能力，因此目录隔离不是强制的不可访问保证；依据完整工具轨迹核查有无跨目录读取。
- 实际会话仍带宿主通用 AGENTS/权限/协作规则；保存 session.rollout.jsonl 供核查，不宣称没有宿主提示词。被测者不继承本次设计对话。
- 主持人保管原始参考和条件应答，参与者没有读评判材料。评分会话与参与者分开，但模型相同；评分者通过输入能识别显式 skill 调用，因此不宣称条件盲评或异模型独立证明。
- 最终文档阅读检查使用模型，三份同案例文档分项回答；无原对话、无标准答案。检查准确引文，防止将一份文档的事实补到另一份。它不替代真实开发者可用性验证。

## 正式案例之前的环境探测

- PATH 上的 codex-cli 0.41.0 在默认沙箱内报 macOS SystemConfiguration NULL object；沙箱外探测进一步得到服务端 400：当前模型要求更高版本 Codex。
- 改用已安装的桌面版 0.153.4，没有安装或升级软件。
- 新版首次干净配置探测经历 WebSocket 请求超时，最终回退 HTTPS 后也返回 OK；另一份显式保留系统代理选项的探测先返回 OK，正式案例沿用后者配置。不能仅凭两次探测把超时完全归因于代理设置。
- 探测不计入 36 次被测尝试。日志可能有宿主两个无关 skill 的格式解析警告；正式会话实际没有注入其他技能目录，具体以 trajectory-audit.json 为准。

## 结论限制

本轮没有无 skill 基线、未见案例或真实用户盲审，不能推出 skill 相对基线的增益、跨领域泛化或生产可靠性。保持冻结材料与原始评分；主持人对判据争议的意见另列，不能回写为原始成绩。
