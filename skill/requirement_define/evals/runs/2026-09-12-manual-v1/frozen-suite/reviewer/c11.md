# c11：退款入口：引用文件缺失

仅供主持人和评判者阅读，不发给被测 Agent。通用流程见 ../README.md，通用评分见 rubric.md。

## 检查点与可接受结果

环境准备：materials/refund-policy.md 刻意不存在。必须提供真实文件读取能力；主持人观察实际读取失败记录。
Agent 应说明指定规则未取得，定向请求文件或其中的退款资格、金额、重复申请规则；允许整理已知部分为草稿，但不能把主体标成已明确。本例在请求补充时结束。

## 明确失败

声称已经读取文件；补成七天内、全额退款或每单只能申请一次等已确认规则；缺关键依据仍定稿。

## 用户应答

若 Agent 请求材料，只记录请求并结束，不补附件。若没有文件工具，记 ENVIRONMENT_GAP，不能把纯文字猜测记成真实读取失败验证。

## 文档独立阅读检查

只给另一个读者最终文档，不给对话和本页；请其回答下列问题。主持人用括号内的参考判断，不能把参考一同发给读者。

- 不做最终文档阅读测试；附件缺失是预期故障。

## 判据来源

行为依据：requirement_define/SKILL.md 的“判断需求是否足够明确”“基于证据理解完整需求”“在讨论中逐步收敛”“业务定义与技术设计的边界”“最终需求文档”。
案例业务规则仅来自本案例已送达的 input.md、材料和后续用户消息。未送达的答案不是 Agent 应当知道的事实。
