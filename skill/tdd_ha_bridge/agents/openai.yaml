interface:
  display_name: "TDD Human-Approval Bridge"
  short_description: "为普通开发者讲清测试范围、场景、结果、缺口与待决策项"
  default_prompt: "Use $tdd-ha-bridge to produce a complete plain-language developer review document that explains the validation scope, lens coverage, concrete scenarios, expected and actual behavior, unproven risks, and required decisions."
policy:
  allow_implicit_invocation: true
