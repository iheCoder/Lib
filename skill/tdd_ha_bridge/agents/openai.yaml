interface:
  display_name: "TDD Human-Approval Bridge"
  short_description: "让普通开发者快速看懂测试覆盖、缺口与待决策项"
  default_prompt: "Use $tdd-ha-bridge to produce a plain-language developer review pack that makes lens coverage, unproven risks, and required decisions obvious before implementation."
policy:
  allow_implicit_invocation: true
