interface:
  display_name: "TDD Human-Approval Bridge"
  short_description: "Design reviewable tests and agent evals with fidelity"
  default_prompt: "Use $tdd-ha-bridge to produce a human-reviewable test or agent-eval design, verification strategy, and adequacy critique before implementation."
policy:
  allow_implicit_invocation: true
