我们有一个内部报表工作台，约 20 人使用，总共约 200 张报表。服务端是一个 Go 服务，数据存在 PostgreSQL；已有登录、按权限查询报表列表的接口，也有保存个人设置的现成接口和表。

想增加“收藏报表”：每个人可以收藏或取消收藏自己能看到的报表，重新登录或换设备后还保留。列表先显示自己收藏的报表，两组内部继续使用原有排序。收藏只影响自己的列表；某张报表后来没有权限了，现有列表接口会过滤它。两个设备操作同一张报表时，以服务端后完成的保存为准。

帮我做一下这个功能的技术设计。
