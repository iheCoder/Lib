# 运行记录

用户已明确同意按所讨论路线修改并运行评估。生产 Skill 修改前，上一轮 `2026-09-13/audit.py check` 已通过；旧证据保持不变。该旧脚本包含“生产文件等于旧快照”的断言，因此不能在新版落地后用这个旧断言评价旧证据完整性。

候选最初改动为 21 行新增及一处改写，`quick_validate.py skill/tech_design` 通过。旧版、候选、题目、评分规则、前置审查和校准文件在 30 条轨迹开始前由 `run_eval.py freeze` 冻结。

## 统一执行要求

各轨迹使用 `collaboration.spawn_agent`，`fork_turns=none`，未指定模型或推理档位。每条轨迹有独立临时目录，只在当前轮次提供 `input.md`；有 Skill 的两组统一使用文件名 `guidance.md`，内容分别来自冻结快照。宿主不暴露精确模型或采样参数，因此报告为未知，不从模型自述推断。

首轮统一要求：在独立对话中处理当前用户技术设计请求，只读该目录获准文件；不读记忆、仓库、其他 Skill、相邻目录或未来输入，不访问外网或其他代理；不实际实现业务或产生外部操作。自然中文回答，需要讨论时把问题写入回答，不调用真人提问工具；完整回答保存 `turn-1.md`，不写实验或分组说明，返回路径。有 Skill 组额外要求依据 `guidance.md`，无 Skill 组明确不加载 Skill。

第二轮由同一代理继续，仅递交新 `input.md`，要求承接自身先前回答，将完整回复写为 `turn-2.md`，其余边界不变。固定追问不一定回答其首轮的所有设计问题；评分须保留这一限制，不把未回应的确认视为已经同意。

这是上下文和指令隔离，文件系统仍共享。原始用户可见回答被完整保存；工具动作没有完整导出日志，不能把隔离声明解释为操作系统级证明。

## 匿名与审计

分组映射只在 root 可读的实验管理约定中保留，评估员仅被允许读取无条件标签的案例包、规则和自己评分。每条轨迹仍有两轮时序和原始用户输入。若发现显式 Skill 自报，匿名副本只去除此类自报，原始回答不改，并在本文件登记。除此之外不做措辞润色或修复。

校准：评分员 a、b 各自 8/8 匹配预设人工合成例。这不能代替对真实复杂轨迹的校准或真人理解测试。评分员会重新读取冻结后的 rubric；冻结版本加入了未覆盖独立分类及真实替代方案维度，原校准八题的预期不变。

## 完成记录

30 条独立轨迹、54 份回答全部完成，无重跑与样本排除。两位评分员各评 30 条，全部评分保留。共登记 10 处显式 Skill 自报删除，匿名副本之外的原文不改；详见 grading/anonymization.json。最终检查通过 20 个冻结文件、54 个回答哈希、60 份评分和 1044 处逐字引文。六处等级分歧保留于 summary.json 和主报告，未重写原始评分。
