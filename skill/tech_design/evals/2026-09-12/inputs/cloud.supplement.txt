把剩下的边界补齐：恢复只回原位置；原父目录不可用或与现有文件重名时，整次恢复失败并保留回收站状态，不自动换位置或覆盖。接收者移除共享入口也进自己的回收站，30 天内可以恢复，恢复时按当前权限检查；同一个原文件的其他入口不受影响。每个文件节点独占一个不会复用的对象存储标识，不存在秒传去重或多个节点共用物理对象；到期回收站节点不能再编辑或恢复内容，物理删除对象不存在可视为成功。所有签发入口都遵守你解释的数据库协调规则，关键检查从主库读取，不用缓存或延迟副本作放行依据。我接受前面已解释的机制和模型以及它们的代价，其余不影响主体方案的细节由你决定。
